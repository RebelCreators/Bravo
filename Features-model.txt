Features - client
settings
  -- push notifications
  -- background updates
  -- email
  -- reset password
  	  -- extenal
  	  -- old passsword
  -- credit card
  	-- token
  	-- approved
  -- bank
  	-- name
  	-- token
  	-- approved
  -- linked accounts
  -- hide profile
  -- blocked users
     -- userID
  -- past services
    -- ""
    -- ""
  -- subscription?
  -- phone number

profiles
	-- My profile
		-- userID
		-- email
		-- about me 
		-- first name
		-- last name
		-- main profile image
		-- location
		-- flaggedBy
   	      -- userID
   	      -- ""
		-- verified
    -- trust score
	-- helpers profile
  -- max radius
  -- generalAvailablity
      -- dayOfWeek
      -- startTime
      -- endTime
      -- exclusions
         -- date
         -- startTime
         -- endTime
         -- allDay

	   -- flaggedBy
   	      -- userID
   	      -- ""
		-- location
	    -- user Id
		-- user online status
		-- main profile image
		-- other profile images
		-- stats
		-- max distance
		--services
			-- id
		    -- name
		    -- description
		    -- hourly rate
		-- detail ratings
			-- user Id
			-- rating
	-- trust score
	-- overall ratings
	-- about me
    --services(helper)
    -- distance from me

services {
-- date
 -- service detail
 -- flaggedBy
    -- userID
    -- ""
 -- coments
 -- name
 -- serviceOwner  - ["Client", "Helper"]
 -- ownerID
 -- name
 -- ownerID
 -- description
 -- hourlyRate
}

ServiceRequest {
-- clientID
-- [helperID]
-- serviceID
-- location
-- hourlyRate
-- duration
-- comments  (may be chat here)
-- clientCompleted
-- helpers completed [helperIDs]
-- client Rating
-- helpersStatuses [{ helper -> "ready", "onway", "arrived", "completed", "canceled"}]
-- status - ["canceled", "pending", "accepted", "completed"]
-- ServiceCharge
-- canceledBy - ["Client", "Helper"]
-- time
-- date
}


ServiceCharge {
  service,
  helpers: [],
  client_id
}

Rating

getCurrentUserClientServiceRequests
getCurrentUserHelperServiceRequests

submitServiceRequest 
acceptServiceRequest / if is helper and "pending"
rejectServiceRequest / if is helper and "pending"
cancelServiceRequest 
arrivedAtVenue/ if is in helpers
completeService / if is in helpers or client
addHelpersToServiceRequest / before will be pending once helper is added accteped
rateClient(rating) / not rated & must be helper
rateHelpers(ratings: [{}]) / not rated & user is client & must be in helpers 

Rating {
      -- user - unique
      -- service - unique
      -- comments
      -- rating: 0 - 5
      -- date
      -- rater - unique
}
ratingsForUser(id)
^^^^^
ratingsForCurrentUser
ratingByID(id)
flagRating(id)


trust score
    : factors
     -- number of accounts connected
     -- facebook connected
     -- email approved
     -- phone number verified
    rating
      -- user Id
			-- coments
			-- rating
			-- date
    license approved?
    verifiedPhoto
    bank approved ?
    -- card 

comments:
client and helper informed on job status update



